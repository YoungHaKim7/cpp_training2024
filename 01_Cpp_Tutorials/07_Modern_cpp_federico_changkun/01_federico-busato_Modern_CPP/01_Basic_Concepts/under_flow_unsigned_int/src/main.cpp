#include <iostream>

int main() {
  // int8_t var;
  // std::cout << "input char no.:" << '\n';
  // std::cin >> var;
  // int a = var * 2;

  // std::cout << "\ninput char no.(2x)" << a << '\n';
  // std::cout << "\ninput char no.(x)" << a / 2 << '\n';
  int8_t underflow_var = -20;
  std::cout << "\ninput char no.(x)" << underflow_var << '\n';

  unsigned i = -2;
  std::cout << "\nunsigned i : " << i << '\n';

  return 0;
}
